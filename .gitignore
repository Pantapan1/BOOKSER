# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/

# android/ios не хранятся в репозитории — генерируются в CI (см. .github/workflows/build-apk.yml).
# Если генерируете их локально для разработки — тоже не коммитьте (или уберите эти строки,
# если решите один раз сгенерировать и закоммитить нативные проекты насовсем — тоже нормальный подход).
/android/
/ios/

*.iml
.idea/
